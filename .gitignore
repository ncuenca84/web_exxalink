# Subdominio WHMCS: vive dentro de public_html pero NO forma parte de este
# proyecto. Se ignora para que 'git pull'/'git status' nunca lo toquen.
my.exxalink.com/

# Credenciales reales del sistema de certificados (NO versionar).
certificados/config.php

# Logs de PHP / servidor.
error_log
**/error_log
*.log

# Archivos temporales del sistema.
.DS_Store
Thumbs.db

# Export de entrenamiento (no publicar en la web)
centrodeayuda_entrenamiento.txt
